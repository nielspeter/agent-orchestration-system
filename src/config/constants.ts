/**
 * System-wide constants
 */

// Default model for the system
export const DEFAULT_MODEL = 'claude-3-5-haiku-latest';

// Logging defaults
export const DEFAULT_LOG_DIR = './conversations';
export const DEFAULT_DISPLAY_MODE = 'both';
export const DEFAULT_VERBOSITY = 'normal';

// Safety defaults
export const DEFAULT_MAX_ITERATIONS = 10;
export const DEFAULT_MAX_DEPTH = 5;
export const DEFAULT_WARN_AT_ITERATION = 5;
export const DEFAULT_MAX_TOKENS_ESTIMATE = 100000;

// Session defaults
export const DEFAULT_TIMEOUT = 30000;
